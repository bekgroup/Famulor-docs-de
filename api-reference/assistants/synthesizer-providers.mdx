---
title: "Synthesizer-Provider abrufen"
api: "GET https://app.famulor.de/api/user/assistants/synthesizer-providers"
icon: "volume-2"
description: "Verfügbare TTS- (Text-to-Speech-) Synthesizer-Provider abrufen"
---

Dieser Endpunkt gibt eine Liste verfügbarer Synthesizer-Provider zurück, die beim Erstellen oder Aktualisieren von Assistenten verwendet werden können. Verwenden Sie die zurückgegebene `id` als `synthesizer_provider_id` bei der Assistenten-Konfiguration.

### Query-Parameter

<ParamField query="language_id" type="integer">
  Provider nach Sprachunterstützung filtern. Es werden nur Provider zurückgegeben, die die angegebene Sprache unterstützen. Verwenden Sie den Endpunkt [Sprachen abrufen](/api-reference/assistants/languages), um verfügbare Sprach-IDs zu erhalten.
</ParamField>

### Antwort-Felder

<ResponseField name="data" type="array">
  <Expandable title="Eigenschaften">
    <ResponseField name="id" type="integer">
      Die eindeutige Kennung des Providers. Verwenden Sie diese als `synthesizer_provider_id` beim Erstellen oder Aktualisieren von Assistenten.
    </ResponseField>

    <ResponseField name="name" type="string">
      Der Anzeigename des Providers (z.B. "ElevenLabs", "Azure", "Cartesia")
    </ResponseField>

    <ResponseField name="code" type="string">
      Der interne Code des Providers
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseExample>
  ```json 200 Antwort theme={null}
  [
    {
      "id": 1,
      "name": "ElevenLabs",
      "code": "elevenlabs"
    },
    {
      "id": 2,
      "name": "Azure",
      "code": "azure"
    },
    {
      "id": 3,
      "name": "Cartesia",
      "code": "cartesia"
    }
  ]
  ```

  ```json 200 Gefiltert nach Sprache theme={null}
  [
    {
      "id": 1,
      "name": "ElevenLabs",
      "code": "elevenlabs"
    },
    {
      "id": 3,
      "name": "Cartesia",
      "code": "cartesia"
    }
  ]
  ```
</ResponseExample>

### Hinweise

* Ohne Angabe von `language_id` werden alle Provider zurückgegeben
* Nicht jeder Provider unterstützt alle Sprachen – nutzen Sie den Filter `language_id` für kompatible Provider
* Synthesizer-Provider sind nur für die Modi `pipeline` und `dualplex` relevant (im Modus `multimodal` wird TTS intern verarbeitet)
* Wenn Sie beim Erstellen eines Assistenten `synthesizer_provider_id` nicht setzen, wird der Standard-Provider der Sprache verwendet
