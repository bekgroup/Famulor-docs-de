---
title: "Telefonnummer freigeben"
api: "DELETE https://app.famulor.de/api/user/phone-numbers/{id}"
description: "Gibt eine Telefonnummer frei und kündigt ihr Abonnement"
---


Dieser Endpunkt gibt eine Telefonnummer von deinem Konto frei. Für dedizierte Nummern mit Abonnements wird das Abonnement gekündigt und die Nummer wird am Ende des aktuellen Abrechnungszeitraums freigegeben.

### Pfad-Parameter

<ParamField path="id" type="integer" required>
  Die eindeutige Kennung der freizugebenden Telefonnummer
</ParamField>

### Antwort-Felder

<ResponseField name="message" type="string">
  Erfolgsmeldung, die den Freigabestatus angibt
</ResponseField>

### Freigabeverhalten

Das Freigabeverhalten hängt vom Telefonnummerntyp ab:

| Typ                   | Verhalten                                                                                                |
| ---------------------- | ------------------------------------------------------------------------------------------------------- |
| **Dedicated (normal)** | Abonnement wird gekündigt. Nummer bleibt bis zum Ende des Abrechnungszeitraums aktiv, dann wird sie freigegeben. |
| **SIP**                | Wird sofort aus deinem Konto gelöscht.                                                                  |
| **Caller ID**          | Wird sofort aus deinem Konto gelöscht und vom Telefonieanbieter entfernt.                          |

<Warning>
Sobald eine Telefonnummer freigegeben wurde, kannst du möglicherweise nicht mehr dieselbe Nummer zurückerhalten. Stelle sicher, dass du alle Assistenten oder Kampagnen aktualisierst, die diese Nummer verwenden, bevor du sie freigibst.
</Warning>

### Was passiert mit zugeordneten Ressourcen

Wenn du eine Telefonnummer freigibst:

* Alle **eingehenden Assistenten**, die dieser Nummer zugeordnet sind, haben ihre Telefonnummer entfernt
* Alle **Kampagnen**, die diese Nummer verwenden, haben sie aus ihrer Telefonnummern-Rotation entfernt
* **Anrufhistorie** wird beibehalten und bleibt zugänglich


<ResponseExample>

```json 200 Subscription Cancelled
{
  "message": "Phone number released successfully. It will be removed at the end of the billing period."
}
```

```json 200 Immediately Deleted
{
  "message": "Phone number deleted successfully."
}
```

```json 400 Already Scheduled
{
  "error": "Phone number is already scheduled for release."
}
```

```json 404 Not Found
{
  "error": "Phone number not found."
}
```

```json 500 Error
{
  "error": "Failed to release phone number. Please try again or contact support."
}
```

</ResponseExample>

<Tip>
Passende Seiten: [Introduction](/de/api-reference/introduction) und [Authentication Guide](/de/developers/authentication-guide) und [API Integration Examples](/de/developers/api-integration-examples).
</Tip>
